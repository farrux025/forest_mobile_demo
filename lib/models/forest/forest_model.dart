class ForestDetailsModel {
  String? name;
  List<String>? imageList;
  String? address;
  double? latitude;
  double? longitude;

  ForestDetailsModel(
      {this.name, this.imageList, this.address, this.latitude, this.longitude});
}
